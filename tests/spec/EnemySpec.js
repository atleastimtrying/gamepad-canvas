describe("Enemies", function() {
  var player;
  var enemies;
  
  beforeEach(function() {
    player = new Player();
    enemies = new Enemies();
  });
  
  it("FIX ME can die", function() {
    enemy.die();
    expect(0).toEqual(1);
  });
  
});